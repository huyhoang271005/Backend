package com.example.hello.Feature.Authentication.DataProjection;

import java.util.UUID;

public interface VariantValueInfo {
    UUID getProductId();
    UUID getVariantId();
    UUID getAttributeValueId();
}
